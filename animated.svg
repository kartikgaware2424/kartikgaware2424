<svg viewBox="0 0 600 200" xmlns="http://www.w3.org/2000/svg">
  <style>
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes wave {
      0%, 100% { transform: rotate(0deg); }
      50% { transform: rotate(20deg); }
    }

    .wave-hand {
      animation: wave 1s infinite alternate;
      transform-origin: center;
    }

    .text {
      font-family: Arial, sans-serif;
      font-size: 24px;
      font-weight: bold;
      fill: white;
      opacity: 0;
      animation: fadeIn 2s forwards, float 3s infinite ease-in-out;
    }

    .name { animation-delay: 0.5s; }
    .title { animation-delay: 1.5s; }
    .passion { animation-delay: 2.5s; }
  </style>

  <rect width="100%" height="100%" fill="#282c34" />

  <text x="50" y="50" class="text name">👋 Hi, I'm Kartik Rajaram Gaware</text>
  <text x="50" y="90" class="text title">🚀 B.Tech CSE | Cyber Security & Forensics | MERN Stack Developer</text>
  <text x="50" y="130" class="text passion">🎯 Passionate about Cybersecurity, Full-Stack Development, and Problem-Solving</text>
  
  <text x="10" y="50" font-size="30px" class="wave-hand">👋</text>
</svg>
